

Each Chapter contains one or more projects (folders).

Each project contains a ReadMe.txt file that describes the process for
building, deploying, and undeploying its components.

The projects uses Apache Ant for the build.

All common libraries are packaged in the 'lib' folder and all project
specific libraries are packaged in the project's 'lib'
folder.


